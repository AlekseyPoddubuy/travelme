<%= form_with(model: metatag, local: true) do |form| %>
  <% if metatag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(metatag.errors.count, "error") %> prohibited this metatag from being saved:</h2>

      <ul>
      <% metatag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :metatag_title %>
  </div>

  <div class="field">
    <%= form.label :keywords %>
    <%= form.text_field :keywords, id: :metatag_keywords %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :metatag_description %>
  </div>

  <div class="field">
    <%= form.label :page %>
    <%= form.text_field :page, id: :metatag_page %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
